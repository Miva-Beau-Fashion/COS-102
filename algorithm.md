STEP 1: START  
STEP 2: INITIALIZE INTEGER VARIABLES `angle1`, `angle2`  , and `angleEval`  
STEP 3: COLLECT AND STORE FIRST ANGLE IN `angle1`  
STEP 4: CHECK IF `scanf` REJECTS INVALID INPUT FOR `angle1`  
STEP 6: IF INVALID, PRINT ERROR, CLEAR INPUT BUFFER, AND RETRY FROM STEP 3  
STEP 7: COLLECT AND STORE SECOND ANGLE IN VARIABLE INTEGER, `angle2`  
STEP 8: CHECK IF `scanf` REJECTS INVALID INPUT FOR `angle2`  
STEP 9: IF INVALID, PRINT ERROR, CLEAR INPUT BUFFER, AND RETRY FROM STEP 8  
STEP 10: EVALUATE AND STORE `180 - angle1 - angle2` IN `angleEval`  
STEP 11: Evaluate
-  IF `angle1 = 90`, `angle2 = 90` or `angleEval = 90`, PRINT "Triangle IS right angled"  
- ELSE PRINT "Triangle is NOT right angled"

STEP 12: END